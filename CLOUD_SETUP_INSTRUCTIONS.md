
# إرشادات تشغيل العيادة أونلاين (مجاناً وبأمان)

لتشغيل العيادة بشكل احترافي ومجاني 100% مع ضمان عدم ضياع البيانات، نحتاج لخدمتين: **قاعدة بيانات** (للحفظ) و **خدمة تخزين صور**.

## الخطوة 1: قاعدة البيانات (لبيانات المرضى)
سنسخدم خدمة **Neon.tech** (أفضل خدمة مجانية لقواعد بيانات Postgres).

1. ادخل على موقع [neon.tech](https://neon.tech) وسجل حساباً مجانياً.
2. أنشئ مشروعاً جديداً "New Project" (سمه مثلاً `eslam-clinic`).
3. ستظهر لك صفحة بعد الإنشاء تحتوي على **Connection String**. انسخ الرابط الذي يشبه هذا:
   `postgres://eslam:password@ep-cool-frog-123.us-east-2.aws.neon.tech/neondb`

## الخطوة 2: تخزين الصور (للأشعة والمرفقات)
سنستخدم خدمة **Cloudinary** (لأن سيرفرات Hugging Face المجانية تمسح الصور كل فترة).

1. ادخل على [cloudinary.com](https://cloudinary.com) وسجل حساباً (مجاني للأبد).
2. في الـ Dashboard الرئيسية، انسخ رابط **"API Environment variable"**. شكله مثل هذا:
   `cloudinary://12345678:abcdefg@my_cloud_name`

---

## الخطوة 3: ربط كل شيء في Hugging Face

1. اذهب لمساحة العمل الخاصة بك (Space) على Hugging Face.
2. ادخل على **Settings** في الأعلى.
3. انزل لقسم **Variables and secrets**.
4. اضغط **New Secret** وأضف ما يلي:

| الاسم (Name) | القيمة (Value) | الوصف |
| :--- | :--- | :--- |
| `DATABASE_URL` | الصق رابط Neon هنا | يبدأ بـ postgres://... |
| `CLOUDINARY_URL` | الصق رابط Cloudinary هنا | يبدأ بـ cloudinary://... |

> **ملاحظة مهمة:** في رابط `DATABASE_URL`، تأكد أنه يبدأ بـ `postgresql://` وليس `postgres://`. إذا كان `postgres`، فقط أضف حرف `ql` ليصبح `postgresql`.

5. بمجرد إضافة هذه الأسرار، سيقوم السيرفر بإعادة التشغيل تلقائياً.

---

### مبروك! 🎉
الآن، مهما حدث للسيرفر، بياناتك محفوظة في Neon وصورك محفوظة في Cloudinary ولن تضيع أبداً.
